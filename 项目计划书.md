# 智慧旅游数字人导览系统 — 项目计划书

---

## 一、项目概述

### 1.1 项目背景

针对智慧旅游场景，构建一个基于大模型和多模态技术的数字人导览系统。游客可通过语音或文字与数字人交互，获取景区讲解、路线推荐等服务；管理方可维护知识库、配置数字人形象、查看运营数据分析报告。

### 1.2 核心目标

| 指标 | 目标值 |
|------|--------|
| 事实性问答准确率 | ≥ 90%（基于标准测试集） |
| 语音交互端到端延迟 | < 5 秒 |
| 数字人表现 | 口型同步、表情配合 |
| 系统稳定性 | 无崩溃或长时间无响应 |

---

## 二、需求分析

### 2.1 游客交互侧

1. **多模态交互**：支持语音输入和文本输入，数字人以语音、表情和口型同步方式回答
2. **智能问答与讲解**：准确回答景区历史、文化、景点特色等常见问题
3. **个性化推荐**：根据游客兴趣（历史/自然/美食）推荐不同游览路线和讲解深度

### 2.2 管理后台侧

1. **知识库管理**：上传、更新景区讲解词、文史资料、FAQ 等知识文档
2. **数字人形象管理**：配置数字人外观、服装、声音等
3. **游客感受度报告**：分析交互记录，生成关注点分析、情感趋势、服务建议
4. **数据大屏**：展示服务人次、热门问答、满意度趋势等运营数据

### 2.3 非功能性需求

- 使用至少 1 个多模态大模型作为核心 AI 支撑
- 构建本地景区知识库，确保回答准确性和自然度
- 交互延迟 < 5s，系统稳定可扩展
- （加分项）GPS 弱信号环境下提供降级定位方案

---

## 三、技术方案

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    前端层                             │
│  游客端: React + Vite H5      管理端: React + Ant Design│
│  ┌──────────┐ ┌──────────┐                         │
│  │Live2D数字人│ │数据大屏   │                         │
│  └──────────┘ └──────────┘                         │
└──────────────┬──────────────────┬───────────────────┘
               │   WebSocket/SSE  │  HTTP REST
┌──────────────▼──────────────────▼───────────────────┐
│                   业务服务层 (FastAPI)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │对话服务   │ │知识库服务 │ │推荐服务   │             │
│  └──────────┘ └──────────┘ └──────────┘             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │数字人服务 │ │分析服务   │ │用户服务   │             │
│  └──────────┘ └──────────┘ └──────────┘             │
└──────────────┬──────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────┐
│                   AI 能力层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │LLM多API  │ │ASR       │ │TTS       │ │RAG     │ │
│  │混合路由   │ │Whisper   │ │CosyVoice │ │检索引擎 │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
└──────────────┬──────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────┐
│                   数据层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │Milvus    │ │PostgreSQL│ │Redis     │ │MinIO   │ │
│  │向量数据库 │ │关系数据库 │ │缓存       │ │对象存储 │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
└─────────────────────────────────────────────────────┘
```

### 3.2 技术栈选型

| 层级 | 选型 | 说明 |
|------|------|------|
| **前端框架** | React 18 + TypeScript + Vite | 构建速度快，生态成熟 |
| **UI 组件库** | Ant Design 5（管理端） + 自定义样式（游客端） | 管理端标准化，游客端个性化 |
| **数字人渲染** | Live2D Cubism SDK for Web + pixi-live2d-display | 2D数字人驱动，支持口型表情 |
| **后端框架** | Python 3.11 + FastAPI | 异步支持好，AI 生态无缝衔接 |
| **LLM 主模型** | DeepSeek-V3 API | 中文能力强，成本低（¥1/百万token） |
| **LLM 视觉** | Qwen-VL-Max | 拍照识景点场景 |
| **LLM 情感/校验** | 豆包 Lite API | 免费额度大，轻量任务 |
| **LLM 长文总结** | 通义千问 LongContext | 周报/月报生成 |
| **ASR 语音识别** | faster-whisper (medium) | 开源，中文识别优秀 |
| **TTS 语音合成** | CosyVoice | 阿里开源，中文自然度最佳 |
| **Embedding** | BGE-M3 (BAAI) | 中文向量化效果最好 |
| **Reranker** | BGE-Reranker-v2-m3 | 检索精排 |
| **向量数据库** | Milvus | 支持混合检索，国产开源 |
| **关系数据库** | PostgreSQL 16 | 成熟稳定 |
| **缓存** | Redis 7 | 热点问答缓存、Celery 队列 |
| **对象存储** | MinIO | 自建 S3 兼容存储 |
| **任务队列** | Celery + Redis | 文档索引等异步任务 |
| **实时通信** | WebSocket + SSE | FastAPI 内置支持 |
| **可视化** | ECharts 5 | 数据大屏图表 |

### 3.3 LLM 多 API 混合路由策略

```
用户请求
    │
    ▼
[路由器] ─── 判断任务类型
    │
    ├── 常规问答（历史/文化/景点）─── DeepSeek-V3 (¥1/百万token)
    ├── 图片识别（拍景点问是什么）─── Qwen-VL-Max
    ├── 情感分析 / 事实校验 ──────── 豆包 Lite (免费额度)
    ├── 长文总结（周报/月报） ────── 通义千问 LongContext
    │
    └── 某API不可用 → 自动降级到备用模型
```

**选型理由**：
- DeepSeek-V3 中文强且便宜，日常问答主力，但视觉能力弱
- Qwen-VL-Max 图片理解好，拍照识景点必备
- 豆包免费额度大，情感分析、内容审核等轻量任务放这里
- 通义千问 LongContext 适合长文档总结
- 多 API 互为备份，单点故障不影响服务
- 统一路由层封装，切换模型不改业务代码

---

## 四、核心模块设计

### 4.1 多模态交互管道（延迟目标 < 5s）

```
语音输入 ──→ [ASR Whisper] ──→ 文本  (~500ms)
                │
                ▼
           [FAQ 快速匹配] ──→ 命中？直接返回  (~50ms)
                │ 未命中
                ▼
           [RAG 混合检索] ──→ 向量检索 + BM25 + RRF融合 + 重排序  (~300ms)
                │
                ▼
           [LLM 流式生成] ──→ SSE 流式输出  (~2s)
                │
        ┌───────┴───────┐
        ▼               ▼
   [TTS CosyVoice]  [数字人驱动]
   语音合成 (~1s)    口型表情 (~1s)
        │               │
        └───┬───┘
            ▼
    前端同步播放音频 + 数字人动画
```

**优化策略**：
- LLM 流式输出 → 分句触发 TTS → 前置音频缓冲区（流水线并行）
- 热点问答 Redis 缓存（跳过 LLM + TTS）
- ASR 音频流式传输，边录边识别

### 4.2 RAG 知识库（准确率 ≥ 90% 的关键）

```
景区资料 (PDF/Word/MD)
    │
    ▼
[文档解析] → PyMuPDF / python-docx → 纯文本
    │
    ▼
[智能分块] → 段落+句子边界切分，chunk_size=512，overlap=64
    │
    ▼
[向量化] → BGE-M3 Embedding → 存入 Milvus
    │
    ▼
[混合检索]
  ├── 向量检索: Milvus IVF_FLAT, top_k=10
  ├── BM25 关键词: jieba 分词 + rank-bm25
  ├── 融合策略: RRF (Reciprocal Rank Fusion)
  └── 重排序: BGE-Reranker-v2 精排 top-5
    │
    ▼
[LLM 回答 + 事实校验]
  ├── System Prompt: "基于以下资料回答，不要编造"
  └── 豆包交叉校验: 回答是否与资料一致
```

**保 90% 准确率的关键措施**：

1. **FAQ 白名单**：高频事实性问题（票价、时间、交通）存 MySQL 精确匹配
2. **混合检索**：向量 + BM25 互补，避免遗漏关键词命中的结果
3. **重排序**：BGE-Reranker 二次精排，确保最相关内容排在前面
4. **事实校验**：LLM 回答后用豆包做一致性验证
5. **版本管理**：知识库内容更新后自动触发重新索引

### 4.3 数字人驱动

```
TTS 音频流 (含音素时间戳)
    │
    ▼
[Live2D 引擎] (pixi-live2d-display + React)
    │
    ├── 口型同步 (Viseme Mapping)
    │   音素 → 口型映射表 (15种) → ParamMouthOpenY
    │   requestAnimationFrame 平滑插值
    │
    ├── 表情系统
    │   微笑(欢迎) / 惊讶(有趣知识) / 点头(认同)
    │   由 LLM 回答的情感分析触发
    │
    └── Idle 动画
        呼吸 / 眨眼 / 小幅身体晃动
        空闲状态自动播放
```

### 4.4 个性化推荐

```
游客画像
  ├── 显式输入: "我对历史感兴趣"
  ├── 隐式推断: 对话关键词分析 → 兴趣标签
  └── 行为记录: 浏览偏好 / 点赞点踩
    │
    ▼
[推荐引擎]
  ├── 路线匹配: 游客兴趣向量 + 景点标签向量的余弦相似度
  ├── 预置路线: 历史线 / 自然线 / 亲子线 / 全景线
  └── 讲解自适应: 追问次数多 → 深度模式；简单提问 → 精简模式
```

### 4.5 管理后台

| 模块 | 功能 |
|------|------|
| **知识库管理** | 文档上传（拖拽+进度条）、分块预览、FAQ 编辑器、重新索引 |
| **数字人配置** | 外观切换（服装/发型）、声音选择（TTS 音色试听）、欢迎语编辑、Live2D 预览 |
| **感受度报告** | 情感趋势折线图、关注点词云、知识库盲区发现、服务建议（LLM 生成） |
| **数据大屏** | 今日/本周服务人次、实时交互监控（WebSocket）、热门问答 Top10、满意度趋势 |

### 4.6 GPS 弱信号处理（加分项）

```
定位策略（优先级递减）:
  1. GPS 定位（室外正常情况）
  2. 二维码定位（景区各点位贴二维码，扫码确定位置）
  3. 手动地图选点（用户在地图上点选当前位置）
  4. 蓝牙 Beacon RSSI 三角定位（需部署硬件）

位置感知: 知道游客位置后主动推荐附近景点讲解
```

---

## 五、数据库设计

### 5.1 表结构

```sql
-- 知识文档表
knowledge_docs (
    id SERIAL PRIMARY KEY,
    title VARCHAR(500) NOT NULL,
    content TEXT NOT NULL,
    file_type VARCHAR(20) NOT NULL,  -- pdf / docx / md / txt
    file_path VARCHAR(1000),
    status ENUM('pending','indexing','indexed','failed'),
    chunk_count INT DEFAULT 0,
    version INT DEFAULT 1,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 知识分块表（对应 Milvus 向量）
knowledge_chunks (
    id SERIAL PRIMARY KEY,
    doc_id INT NOT NULL REFERENCES knowledge_docs(id),
    chunk_index INT NOT NULL,
    chunk_text TEXT NOT NULL,
    embedding_id VARCHAR(200),  -- Milvus 主键
    token_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW()
);

-- FAQ 精确匹配表
faq_entries (
    id SERIAL PRIMARY KEY,
    question TEXT NOT NULL,
    answer TEXT NOT NULL,
    keywords TEXT,  -- 逗号分隔关键词
    category VARCHAR(100) DEFAULT 'general',
    priority INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    hit_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 交互记录表
interaction_logs (
    id SERIAL PRIMARY KEY,
    session_id VARCHAR(100) NOT NULL,
    user_input TEXT NOT NULL,
    input_type VARCHAR(20) DEFAULT 'text',  -- text / voice
    asr_text TEXT,
    retrieved_chunks TEXT,  -- JSON
    llm_response TEXT NOT NULL,
    llm_model VARCHAR(100),
    tts_audio_url VARCHAR(1000),
    sentiment_score FLOAT,
    sentiment_label VARCHAR(20),  -- positive / neutral / negative
    user_feedback VARCHAR(20),    -- like / dislike
    latency_ms INT DEFAULT 0,
    is_faq_hit BOOLEAN DEFAULT FALSE,
    metadata_json JSON,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 数字人配置表
avatar_config (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    model_path VARCHAR(1000),
    appearance_json JSON,     -- 服装/发型/配饰
    voice_id VARCHAR(200),     -- TTS 音色 ID
    emotion_presets JSON,      -- 表情预设
    welcome_message TEXT,
    is_active BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 游客画像表
tourist_profiles (
    id SERIAL PRIMARY KEY,
    openid VARCHAR(200) UNIQUE,
    session_id VARCHAR(100) NOT NULL UNIQUE,
    interests JSON,            -- ["history", "nature", "food"]
    interest_embedding FLOAT[],
    preferences_json JSON,
    visit_history JSON,
    total_interactions INT DEFAULT 0,
    avg_sentiment FLOAT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## 六、项目目录结构

```
smart-tourism/
├── backend/                        # Python FastAPI 后端
│   ├── app/
│   │   ├── api/                    # API 路由
│   │   │   ├── chat.py             # 对话接口（SSE 流式）
│   │   │   ├── knowledge.py        # 知识库管理
│   │   │   ├── avatar.py           # 数字人配置
│   │   │   ├── analytics.py        # 数据分析报告
│   │   │   ├── dashboard.py        # 数据大屏
│   │   │   └── upload.py           # 文件上传
│   │   ├── core/                   # 核心模块
│   │   │   ├── config.py           # Pydantic 配置管理
│   │   │   ├── database.py         # SQLAlchemy 异步引擎
│   │   │   ├── redis_client.py     # Redis 客户端
│   │   │   ├── llm.py              # 多 API 大模型路由
│   │   │   ├── rag.py              # RAG 检索增强生成
│   │   │   ├── asr.py              # Whisper 语音识别
│   │   │   └── tts.py              # CosyVoice 语音合成
│   │   ├── models/                 # SQLAlchemy 数据模型
│   │   │   ├── knowledge.py        # 知识库相关表
│   │   │   ├── interaction.py      # 交互日志表
│   │   │   ├── avatar.py           # 数字人配置表
│   │   │   └── tourist.py          # 游客画像表
│   │   ├── services/               # 业务逻辑层
│   │   │   ├── chat_service.py     # 对话处理
│   │   │   ├── knowledge_service.py # 文档解析分块
│   │   │   ├── recommend_service.py # 个性化推荐
│   │   │   └── analytics_service.py # 数据分析
│   │   ├── tasks/                  # Celery 异步任务
│   │   │   ├── index_task.py       # 文档索引
│   │   │   └── report_task.py      # 报告生成
│   │   └── main.py                 # FastAPI 入口
│   ├── alembic/                    # 数据库迁移
│   ├── data/                       # 景区资料
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/                       # React + Vite 前端
│   ├── src/
│   │   ├── pages/
│   │   │   ├── tourist/            # 游客端
│   │   │   │   ├── ChatPage.tsx         # 对话主界面
│   │   │   │   └── RecommendPage.tsx    # 推荐路线
│   │   │   └── admin/              # 管理端
│   │   │       ├── KnowledgePage.tsx    # 知识库管理
│   │   │       ├── AvatarPage.tsx       # 数字人配置
│   │   │       ├── ReportPage.tsx       # 感受度报告
│   │   │       └── DashboardPage.tsx    # 数据大屏
│   │   ├── components/
│   │   │   ├── DigitalHuman/       # 数字人组件
│   │   │   │   ├── Live2DStage.tsx      # Live2D 画布
│   │   │   │   ├── VoiceInput.tsx       # 语音输入按钮
│   │   │   │   └── ChatBubble.tsx       # 对话气泡
│   │   │   ├── admin/              # 管理端通用组件
│   │   │   └── common/             # 通用组件
│   │   ├── hooks/                  # 自定义 Hooks
│   │   │   ├── useWebSocket.ts
│   │   │   ├── useVoiceRecord.ts
│   │   │   └── useLive2D.ts
│   │   ├── stores/                 # Zustand 状态管理
│   │   ├── api/                    # API 请求封装
│   │   ├── router.tsx              # 路由配置
│   │   └── App.tsx
│   ├── public/live2d-models/       # Live2D 模型文件
│   ├── package.json
│   ├── vite.config.ts
│   └── Dockerfile
├── docs/                           # 项目文档
│   ├── 部署手册.md
│   └── 设计文档.md
├── docker-compose.yml              # 一键部署
└── README.md
```

---

## 七、开发计划（8 周）

### 第 1 周：项目脚手架 + 基础设施

- [ ] 初始化前后端项目骨架
- [ ] 配置 Docker Compose（PostgreSQL、Redis、MinIO、Milvus）
- [ ] 数据库建表 + Alembic 迁移
- [ ] 景区资料解析入库 + 智能分块工具

### 第 2 周：RAG 知识库 + 问答核心（上）

- [ ] BGE-M3 Embedding + Milvus 向量入库
- [ ] 混合检索：向量检索 + BM25 + RRF 融合
- [ ] BGE-Reranker 重排序
- [ ] FAQ 白名单精确匹配 + 模糊匹配

### 第 3 周：RAG 知识库 + 问答核心（下）+ 多模态交互

- [ ] LLM 多 API 路由封装（DeepSeek/Qwen/豆包）
- [ ] SSE 流式对话接口
- [ ] 事实校验（豆包交叉验证）
- [ ] 50 题准确率测试脚本 + 迭代调优

### 第 4 周：多模态交互 + 数字人前端（上）

- [ ] faster-whisper ASR 语音识别
- [ ] CosyVoice TTS 语音合成
- [ ] WebSocket 信令（音频流 + 文本双向通信）
- [ ] 延迟优化（流水线并行 + Redis TTS 缓存）

### 第 5 周：数字人前端（下）

- [ ] pixi-live2d-display 集成 + React 组件封装
- [ ] 口型同步（Viseme Mapping）+ 音素时间戳
- [ ] 表情系统（微笑/惊讶/点头）+ Idle 动画
- [ ] 游客端对话界面（语音按钮 + 对话气泡 + 推荐面板）

### 第 6 周：管理后台 + 个性化推荐

- [ ] 知识库管理页面（上传/列表/分块预览/FAQ 编辑器）
- [ ] 数字人配置页面（外观/声音/欢迎语 + 实时预览）
- [ ] 游客画像构建 + 推荐引擎（路线匹配）
- [ ] 讲解深度自适应

### 第 7 周：数据大屏 + GPS 弱信号 + 性能优化

- [ ] 感受度报告页面（情感趋势/词云/盲区/服务建议）
- [ ] 数据大屏（指标卡片/实时监控/热门排行/自动刷新）
- [ ] GPS 弱信号降级方案（二维码定位/手动选点/Beacon）
- [ ] Locust 压力测试（50 并发 < 5s）
- [ ] 100 题准确率评测报告

### 第 8 周：文档 + 演示视频

- [ ] 产品总体设计文档（架构图 + 模块设计 + 数据流）
- [ ] 部署手册（Docker Compose 一键启动）
- [ ] 方案介绍 PPT（场景分析/方案设计/核心技术/创新点/测试数据）
- [ ] 演示视频录制（3 个核心场景 + 管理后台展示，5-7 分钟）

---

## 八、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| LLM API 调用延迟高 | 交互体验差 | 流式输出 + 分句 TTS 并行，FAQ 快速通道 |
| 知识库检索准确率不达标 | 回答错误 | 混合检索 + Reranker + FAQ 白名单 + 事实校验四重保障 |
| CosyVoice 部署复杂 | 延迟 TTS 集成 | 备选 Edge-TTS，后续再切换 |
| Live2D 模型资源不足 | 数字人效果差 | 使用免费 Live2D 模型，聚焦口型同步效果 |
| 多 API 费用超预期 | 成本不可控 | 豆包免费额度承担轻量任务，Redis 缓存减少重复调用 |

---

## 九、交付物清单

| 序号 | 交付物 | 说明 |
|------|--------|------|
| 1 | 产品源代码 | 完整的 `smart-tourism/` 项目，含 Docker Compose 一键部署 |
| 2 | 部署和使用手册 | `docs/部署手册.md`，环境要求 + 启动步骤 + 功能说明 |
| 3 | 产品总体设计文档 | `docs/设计文档.md`，架构、模块、数据库、接口设计 |
| 4 | 方案介绍 PPT | 需求场景、整体方案、核心技术、创新点、测试数据、团队介绍 |
| 5 | 产品演示视频 | 5-7 分钟，.mp4，3 个场景 + 管理后台完整展示 |

---

## 十、评分项对照

| 评分项 | 满分 | 对应实现 |
|--------|------|----------|
| 多模态交互 | 40 | 语音输入 → ASR → LLM → TTS → 数字人口型同步全链路 |
| 智能问答与讲解 | 40 | RAG 混合检索 + LLM + FAQ 白名单，≥90% 准确率 |
| 管理后台 | 40 | 知识库管理 + 数字人配置 + 报告 + 数据大屏 |
| 数字人技术集成度 | 15 | Live2D 口型同步 + 表情 + Idle 动画 |
| 大模型与知识库应用 | 15 | 多 API 混合路由 + Milvus 向量库 + 混合检索 + 重排序 |
| 行业实用与体验性 | 20 | Web H5 随时可用，延迟 < 5s，数字人自然交互 |
| 文档质量 | 10 | 设计文档 + 部署手册 + PPT + 视频 |
| **合计** | **100** | |
