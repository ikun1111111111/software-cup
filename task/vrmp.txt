根本原因：renderer.setPixelRatio(2) 设置了 2x 的 WebGL 视口，但 expo-gl 实际使用的是 1.25x 像素比。视口 (200x340) 远大于画布缓冲区 (125x212)，导致模型上半部分渲染到了画布可见区域之外。修复：动态计算 actualPixelRatio = gl.drawingBufferWidth / width，确保 Three.js 视口与 expo-gl 的画布缓冲区匹配。-*/zai

 nod：振幅 0.15→0.06，频率 3Hz→2Hz（更轻更慢）
  - shakeHead：频率 4Hz→1.5Hz，振幅 0.12→0.08，结束后 30% 时间内缓慢看向右侧
  - lookUp：偏移 0.5→0.25，防止突然弹回 
  - 所有动作改用 ease-out 缓动曲线（1 - progress²），结束时平滑过渡

  
  1. VRMIdleAnim.ts — applyAction() 新增 case 'wave'：右臂抬起 + 手腕左右摆动，带动视线偏移
  2. textTimeline.ts — Action 类型扩展 + analyzeSentence() 新增 /你好|欢迎|嗨|hi|hello|见到你/ → wave 触发规则
  3. guide-demo.tsx — 底部测试栏新增 👋 按钮（橙色底，带分隔线），点击触发 "你好呀，欢迎来到灵山胜境！" + wave 动画

  大臂 (upperArm)

  ┌─────┬───────────┬──────────────────────────────────────┐
  │ 轴  │   作用    │                 方向                 │
  ├─────┼───────────┼──────────────────────────────────────┤
  │ Z   │ 抬臂/放下 │ 右臂：负值=抬起，正值=放下；左臂相反 │外翻
  ├─────┼───────────┼──────────────────────────────────────┤
  │ X   │ 前后摆动  │ 正值=向前，负值=向后                 │前翻
  ├─────┼───────────┼──────────────────────────────────────┤
  │ Y   │ 内旋/外旋 │ 正值=一个方向，负值=反方向           │
  └─────┴───────────┴──────────────────────────────────────┘

  前臂 (lowerArm)

  ┌─────┬────────────────────┬───────────────────────────────────────────┐
  │ 轴  │        作用        │                   方向                    │
  ├─────┼────────────────────┼───────────────────────────────────────────┤
  │ X   │ 肘部弯曲/伸直      │ 正值=弯曲（手向肩），负值=伸直；idle=0.05 │旋转方向
  ├─────┼────────────────────┼───────────────────────────────────────────┤
  │ Y   │ 小臂沿自身长轴扭转 │ 正/负=手掌朝向旋转                        │绕大臂转
  ├─────┼────────────────────┼───────────────────────────────────────────┤
  │ Z   │ 小臂侧向摆动       │ 正/负=手掌左右分开/合拢                   │内外
  └─────┴────────────────────┴───────────────────────────────────────────┘

  手掌 (hand)

  ┌─────┬────────────┐
  │ 轴  │    作用    │
  ├─────┼────────────┤
  │ X   │ 手腕前后弯 │能朝前
  ├─────┼────────────┤
  │ Y   │ 手腕扭转   │能变平
  ├─────┼────────────┤
  │ Z   │ 手腕左右弯 │能竖直
  └─────┴────────────┘